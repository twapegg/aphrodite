/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "eu.louisvuitton.com",
      "via.placeholder.com",
      "www.louisvuitton.com",
    ],
  },
};

export default nextConfig;
