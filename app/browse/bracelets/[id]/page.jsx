/*import ProductPage from "@/components/ProductPage";
import React from "react";

const page = () => {


  const posts = await fetch('api/bracelets/').then((res) => res.json())
 
  return posts.map((post) => ({
    slug: post.slug,
  }))


  return <ProductPage />;
};

export default page;
 */